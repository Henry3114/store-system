# 数据库连接（本地开发用 PostgreSQL，生产用 Supabase）
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/store_system

# JWT 密钥（生产环境务必修改）
SECRET_KEY=dev-secret-key-change-in-production

# JWT 过期时间（分钟）
ACCESS_TOKEN_EXPIRE_MINUTES=1440
